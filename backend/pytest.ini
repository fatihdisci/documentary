[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q --strict-markers
markers =
    slow: tests that run a real render and take tens of seconds
filterwarnings =
    ignore::DeprecationWarning:starlette.*
